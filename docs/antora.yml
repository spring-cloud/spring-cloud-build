name: cloud-build
version: true
title: Spring Cloud Build
nav:
  - modules/ROOT/nav.adoc
ext:
  collector:
    run:
      command: ./mvnw clean install -Pdocs -pl docs -DskipTests
      local: true
    scan:
      dir: ./target/classes/antora-resources/
